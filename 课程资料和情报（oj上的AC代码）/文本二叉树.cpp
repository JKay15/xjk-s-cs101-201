#include <bits/stdc++.h>
using namespace std;
struct node{
    char name;
    node* left,*right;
};
node build(){


}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin>>t;
    while(t--){
        node root=build();

    }

    return 0;
}